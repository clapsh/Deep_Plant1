import keyId

# RDS Configuration
config = {
    "aws_db": {
        "user": "jeongseonghun",
        "password": "",
        "host": "localhost",
        "port": "5432",  # Maria DB의 포트
        "database": "postgres",
    }
}
